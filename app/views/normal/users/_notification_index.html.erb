<h3 class="page-header">お知らせ一覧</h3>

<div class="well">
  <% @notifications.each do |notification| %>
    <p><%= notification.content %>
    <%= notification.created_at.strftime("%Y/%m/%d") %></p>
  <% end %>
</div>
