<%= form_for(ticket.reverse_invitations.find_by(user_id: current_user.id), url: ticket_invitation_path(ticket.id, ticket.reverse_invitations.find_by(user_id: current_user.id)), method: :delete) do |f| %>
  <%= f.submit '取り消す', class: 'btn btn-danger' %>
<% end %>
